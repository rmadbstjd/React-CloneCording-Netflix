import React from 'react';
import InputRange from 'react-input-range';
const Filterrange = () => {
    return (
        <InputRange
        maxValue={20}
        minValue={0}>
            
        </InputRange>
    );
};

export default Filterrange;